spring:
  application:
    name: identityserver
  datasource:
    url: "jdbc:postgresql://localhost:5432/identity_db"
    username: postgres
    password: test
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    showSql: true
    hibernate:
      ddlAuto: update
server:
  port: 8083